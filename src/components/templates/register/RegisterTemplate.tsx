interface RegisterTemplateProps {
  children: React.ReactNode;
}

export default function RegisterTemplate({ children }: RegisterTemplateProps) {
  return (
    <div>
      yochai templaet bata ho
      {children}
    </div>
  );
}
